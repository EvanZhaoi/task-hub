# TaskHub 技术选型 v0.5

> **项目代号**：TaskHub
> **文档版本**：v0.5
> **最后更新**：2026-06-11
> **状态**：待产品负责人拍板
> **配套文档**：[需求文档 v0.4](./需求文档.md) · [交接记录](./交接记录.md)

---

## 1. 选型原则

| 原则 | 说明 |
|---|---|
| **沿用成熟栈** | Evan 已有 knowledge-assistant、online-exam-system 两套项目，验证过 Vue 3 + Spring Boot 3 + MySQL 8 + Redis + Docker 的组合 |
| **少即是多** | MVP 不引入新语言、新架构；能用标准库的不引依赖；能用云服务的不自建 |
| **接口先行** | 对外接口（人员信息、附件存储）必须用抽象层封装，方便后续替换 |
| **可演示可部署** | 选型必须支持 Mac mini 本地开发 + 单一 Docker Compose 一键起 |

---

## 2. 全栈总览（一表看懂）

| 层 | 选型 | 版本 | 备注 |
|---|---|---|---|
| **前端框架** | Vue 3 | 3.4+ | Composition API + `<script setup>` |
| **前端语言** | TypeScript | 5.x | 严格模式 |
| **构建工具** | Vite | 5.x | Vue 3 官方推荐 |
| **路由** | Vue Router | 4.x | Hash 模式（MVP 简单） |
| **状态管理** | Pinia | 2.x | Vue 3 官方推荐 |
| **样式** | Tailwind CSS | 3.x | 与原型一致 |
| **UI 组件库** | **shadcn-vue** + Reka UI | 2.10 / 2.9 | MIT 商用可，复制粘贴模式，零 bundle 锁定 |
| **富文本** | **TipTap** (推荐) / Quill | 2.x / 1.3.7 | 见 §6 决策 2 |
| **HTTP 客户端** | Axios | 1.x | 拦截器统一处理错误 |
| **Gantt** | **自写 CSS + HTML** | — | MVP 够用，复杂再换库 |
| **后端语言** | Java | 21 LTS | 现代特性 + 长期支持 |
| **后端框架** | Spring Boot | 3.3.x | Java 21 + Jakarta EE 10 |
| **ORM** | **MyBatis-Plus** (推荐) / Spring Data JPA | 3.5.x | 见 §6 决策 3 |
| **鉴权** | **公司 SSO 插件**（token 透传，验证后调用人员接口拿信息） | — | 见 §3.5 集成方案 |
| **API 风格** | RESTful + JSON | — | 不用 GraphQL（MVP 无必要） |
| **数据库** | MySQL | 8.4 LTS | 关系型，事务支持 |
| **缓存 / Session** | Redis | 7.x | 单一用途，集群按需 |
| **消息队列** | **不引入**（MVP） | — | 二期加 RabbitMQ / Redis Stream |
| **任务调度** | Spring `@Scheduled`（MVP） | — | 见 §6 决策 4 |
| **文件存储** | 本地磁盘（抽象 Storage 接口） | — | 后续可换 S3 / OSS |
| **API 文档** | SpringDoc OpenAPI | 2.x | Swagger UI |
| **单元测试** | JUnit 5 + Mockito + Vue Test Utils | — | 后端 + 前端 |
| **CI/CD** | GitHub Actions | — | 双仓（GitHub + Gitee）触发 |
| **镜像仓库** | Docker Hub / 阿里云 ACR | — | 见 §6 决策 5 |
| **部署目标** | **Mac mini 本地**（开发/演示） + **云服务**（生产） | — | 见 §6 决策 5 |
| **监控 / 日志** | Spring Actuator + 本地文件 + 控制台 | — | 后期接 Sentry / ELK |

---

## 3. 前端选型详解

### 3.1 为什么是 Vue 3（不是 React）

| 维度 | Vue 3 | React 18 |
|---|---|---|
| 学习曲线 | ⭐⭐ 平缓 | ⭐⭐⭐ 较陡 |
| Evan 已有经验 | ✅ knowledge-assistant / online-exam-system | ❌ 无 |
| 模板清晰度 | ✅ HTML-like | ❌ JSX（要 Babel） |
| 体积 | 较小 | 较大 |
| 中文社区 | 活跃 | 活跃 |

> **结论**：**Vue 3**。经验复用比新框架收益大。

### 3.2 为什么用 TypeScript

- 大型项目必备
- 提前发现类型错误，减少 runtime bug
- IDE 自动补全
- 招聘 / 协作友好

### 3.3 UI 库：shadcn-vue + Reka UI

**锁定**：**shadcn-vue**（MIT）+ 底层 **Reka UI**（MIT）

**为什么选 shadcn-vue**（修正版）：
- **MIT 开源 + 可商用**
- **复制粘贴模式**：组件代码复制到 `src/components/ui/`，不是 npm 依赖——不会被升级绑架
- **纯 Tailwind 底层**：与我们 Tailwind 配置天然契合，可直接覆盖品牌色 #5e6ad2
- **Reka UI 提供可访问性**：Dialog、Select、Dropdown 等"难做对"的组件由 Reka 管 a11y / 焦点 / 键盘
- **现代美感**：默认 shadcn 风格，干净专业

**架构**：
```
src/components/ui/      ← shadcn-vue 组件（你的代码）
├── Button.vue          ← cva 变体 + cn() 工具
├── Card.vue
├── Input.vue
├── Dialog.vue          ← 包装 Reka UI DialogRoot
└── ... (按需加)

src/lib/utils.ts        ← cn() 工具（clsx + tailwind-merge）
```

**添加新组件**（以后）：
```bash
npx shadcn-vue@latest add select  # 自动加 Select 组件
npx shadcn-vue@latest add tabs    # 加 Tabs
```

**使用**：
```vue
<Button variant="destructive" size="sm">删除</Button>
<Dialog v-model:open="showModal" title="确认删除">
  <p>确定要删除吗？</p>
</Dialog>
```

**不在 shadcn 内的（项目自写）**：
- TaskCard（业务组件）
- BidListItem（业务组件）
- GanttChart（甘特图，特殊交互）
- StatusBadge（状态徽章，定制）
- AppHeader（应用布局）

### 3.4 Gantt：自写 vs 库

| 选项 | MVP 适用度 | 长期 |
|---|---|---|
| **自写 CSS + HTML**（推荐） | ✅ 够用 | 拖拽 / 依赖关系时不够 |
| dhtmlx-gantt | ❌ 商业授权（$$$） | — |
| gantt-task-react | ✅ 免费 | 需适配 Vue |
| vue-ganttastic | ⚠️ 较简单 | — |

> **结论**：**自写**。MVP 只需"展示"不需"编辑"。二期如要拖拽改交期，再评估 dhtmlx 替代品。

---

## 4. 后端选型详解

### 4.1 为什么是 Spring Boot 3 + Java 21

- Evan 10 年 Java 经验，零学习成本
- Spring Boot 3 是 Jakarta EE 10 时代标准
- Java 21 LTS 性能优于 17（虚拟线程已 GA）
- 生态成熟，文档全

### 4.2 ORM：MyBatis-Plus vs Spring Data JPA

| 维度 | MyBatis-Plus | Spring Data JPA |
|---|---|---|
| SQL 可控性 | ✅ 自己写 | ⚠️ 框架生成，难调优 |
| 复杂查询 | ✅ XML / 注解 | ⚠️ Specification / QueryDSL 复杂 |
| 单表 CRUD | ✅ BaseMapper 自动 | ✅ Repository 自动 |
| 性能 | 略优 | 略逊（反射多） |
| 学习曲线 | ⭐⭐ | ⭐⭐⭐ |
| 团队倾向 | 多数国内团队 | 多数外企 |

> **结论**：**MyBatis-Plus**（推荐）。Evan 需拍板确认。**待决策 3**。

### 4.3 后端包结构

```
com.evanzhaoi.taskhub
├── config/          # 配置类（跨域、安全、Swagger）
├── controller/      # REST API
├── service/         # 业务逻辑
│   └── impl/
├── mapper/          # MyBatis Mapper（如果用 MyBatis-Plus）
├── entity/          # 数据库实体
├── dto/             # 请求/响应 DTO
├── enums/           # 枚举（任务状态等）
├── exception/       # 自定义异常
├── common/          # 工具类
├── integration/     # 外部系统对接（人员信息 API、文件存储）
│   ├── personnel/   # 外部人员接口
│   └── storage/     # 文件存储抽象（本地/S3/OSS）
└── TaskHubApplication.java
```

### 4.4 错误处理

- 全局 `@RestControllerAdvice` 统一捕获
- 错误码 + 错误信息 + 业务数据（如有）
- 前端 axios 拦截器统一弹 toast

### 4.5 集成架构（公司 SSO + 人员信息接口）

TaskHub 本身**不实现登录**，通过两个外部接口拿身份信息。

```
业务方调 TaskHub
     │
     ▼
[Request Header: Authorization: Bearer <token>]
     │
     ▼
┌─────────────────────────────────┐
│   Spring 拦截器 / 过滤器          │
│   - 提取 token                   │
│   - 调 SSO 验证接口 / 验签         │
│   - 拿到 userId                  │
│   - 查 Redis 缓存                │
└────────────┬────────────────────┘
             │ 未缓存
             ▼
   调公司人员信息接口
   GET /api/personnel/{userId}
   拿到 name/dept/avatar/email
   写 Redis（TTL 5-10 分钟）
             │
             ▼
   继续 Spring MVC 流程
   Controller 能拿到 UserContext
```

**包结构**：
```
com.evanzhaoi.taskhub.integration
├── sso/
│   ├── SsoClient.java            # 调公司 SSO 验证接口
│   ├── TokenValidator.java       # token 验证接口
│   └── dto/                      # 请求/响应 DTO
├── personnel/
│   ├── PersonnelClient.java      # 调公司人员信息接口
│   ├── UserContext.java          # 缓存中的用户信息
│   └── dto/
└── cache/
    └── UserContextCache.java     # Redis 缓存封装
```

**关键设计原则**：
- 业务代码只依赖 `UserContext`，不直接调外部接口
- 外部接口调用都走抽象层（`SsoClient` / `PersonnelClient`），后续换协议不动业务
- 缓存优先，未命中才调外部

---

## 5. 数据库 & 存储

### 5.1 MySQL 8.4 LTS

- 已用过的稳定版本
- 支持 JSON 列（存富文本 HTML 不用建子表）
- 全文索引（未来做搜索升级用）

### 5.2 Redis 7.x 用途

| 用途 | 存什么 | TTL |
|---|---|---|
| Session | 用户登录态 | 7 天 |
| 验证码 | 短信 / 邮件验证码 | 5 分钟 |
| 限流 | 接口调用计数 | 1 分钟 |
| 任务搜索热词 | 搜索历史 | 30 天 |
| 分布式锁 | 选标操作防并发 | 10 秒 |

> MVP 用单一 Redis 实例（与后端同机器），集群按需。

### 5.3 文件存储

**MVP**：本地磁盘
- 路径：`/data/taskhub/uploads/`
- 通过 `StorageService` 接口封装
- 抽象层：未来切换到 S3 / 阿里云 OSS 不动业务代码

```java
public interface StorageService {
    String upload(MultipartFile file, String bizType);  // 返回文件 key
    InputStream download(String key);
    void delete(String key);
    String getUrl(String key);
}
```

实现：
- `LocalStorageService`（MVP）
- `S3StorageService`（二期）

### 5.4 数据库设计原则

- 主键：雪花 ID（long，分布式友好）
- 软删：`deleted_at` 字段，不真删
- 审计：`created_at`, `created_by`, `updated_at`, `updated_by`
- 状态：枚举 + 注释（前端要同步）
- JSON 字段：富文本描述、协作成员列表
- 索引：状态、created_at、payment_account_id、created_by

---

## 6. 关键开放决策（需 Evan 拍板）

> 以下 5 个决策对系统形态有重大影响，必须先定。其他技术细节可开发过程中再定。

### 决策 1：鉴权方式 ✅ 已定（公司插件）

**结论**：TaskHub **不做登录、不发 token、不管密码**。统一接入公司现有 SSO 插件。

- 业务方登录 → 公司 SSO 插件颁发 token
- 业务方调 TaskHub API → 请求头带 `Authorization: Bearer <token>`
- TaskHub 验证 token 有效性 → 拿到 userId → 调公司人员信息接口拿详细资料
- 业务代码只关心"当前 user 是谁"，不关心怎么登录

**具体方案**（与 Evan 确认后细化）：
- **token 验证**：调公司 SSO 验证接口（1 次 HTTP）或本地验证 JWT 签名（二期）
- **人员信息**：独立调用人员信息接口（与验证接口可能同源也可能分开）
- **结果缓存**：Redis 缓存用户信息 5-10 分钟（避免每次请求都调外部）
- **撤销**：依赖 SSO 插件自身机制，TaskHub 不额外处理

**接口需求清单**（需 Evan 提供）：
- [ ] SSO 验证端点 URL + 协议（REST / gRPC）
- [ ] token 格式示例（JWT? opaque?）
- [ ] 人员信息接口是否返回部门、邮箱、头像等
- [ ] 错误码约定（token 过期 / 无效 / 权限不足）

> 实现代码隔离在 `integration/sso/` 与 `integration/personnel/` 两个包，业务代码不依赖具体协议。

### 决策 2：富文本编辑器 ✅ 已定

**锁定**：**A. TipTap**

**选型理由**：
- 基于 ProseMirror（业界标准），Vue 3 集成成熟
- 扩展性强：@ 提及 / 表格 / 协作编辑等二期功能易加
- 社区活跃，文档中文友好

**协议核对**（2026-06-11 确认）：
- TipTap core：MIT
- ProseMirror：MIT
- StarterKit / Link / Placeholder 等官方扩展：MIT
- **✅ 开源 + 可商用**

**必用扩展**（v0.3 锁定的功能矩阵）：
- `@tiptap/starter-kit`（粗体 / 斜体 / 下划线 / 删除线 / 标题 / 列表）
- `@tiptap/extension-link`（链接）
- `@tiptap/extension-placeholder`（占位符）

**二期可加**：
- `@tiptap/extension-mention`（@ 提及人）
- `@tiptap/extension-table`（表格）
- `@tiptap/extension-image`（图片）

### 决策 3：ORM 选型 ✅ 已定

**锁定**：**A. MyBatis-Plus**（Apache 2.0）

**选型理由**：
- SQL 可控：任务搜索 LIKE + 复合索引能写得很精准
- 报告 / 统计查询方便
- 国内社区成熟，问题好查
- BaseMapper 自动覆盖单表 CRUD

**关键依赖**：
- `mybatis-plus-spring-boot3-starter`（适配 Spring Boot 3）
- `mybatis-plus-jsqlparser`（复杂 SQL 解析）
- 分页插件 `PaginationInnerInterceptor`

**避坑**：
- 多租户 / 软删等场景慎用插件
- 复杂 SQL 仍写 XML 注释清楚

### 决策 4：任务调度 ✅ 已定

**锁定**：**A. Spring `@Scheduled`**（MVP） + **C. XXL-JOB**（二期）

**MVP 场景**：
- 每天 00:30 扫描 OPEN 状态任务，超时无投标 → 自动 FAILED
- 每周一清理软删超过 30 天的任务

**实现位置**：
- `@EnableScheduling` 放在主启动类
- 调度方法放 `com.evanzhaoi.taskhub.scheduler` 包
- 配合 `@Async` 避免阻塞调度线程

**二期升级**（XXL-JOB）：
- 分布式调度
- 任务失败重试
- 可视化运维

### 决策 5：部署目标 ✅ 已定

**锁定**：**A. Mac mini 本地**（开发/演示） + **B. 云 ECS**（生产）

**架构路径**：
1. **开发/演示阶段**（0 → 内部试运行）
   - Mac mini 本地 Docker Compose
   - Cloudflare Tunnel 暴露公网（已有 Playbook）
   - 域名 `<project>.demo.<rootdomain>`
2. **生产阶段**（试运行 → 全员上线）
   - 迁到云 ECS（2C4G 起步，按量扩容）
   - MySQL 用云 RDS（减少运维）
   - Redis 用云托管（减少运维）
   - 文件存储迁到 OSS / S3

**Docker Compose 服务清单**（本地开发）：
- `taskhub-backend`（Spring Boot 应用）
- `taskhub-frontend`（Nginx + Vue 静态资源）
- `mysql`（8.4 LTS）
- `redis`（7.x）
- `adminer`（数据库管理，可选）

**生产环境待决**（二期）：
- 镜像仓库：阿里云 ACR / Docker Hub
- 日志：云日志服务（SLS）
- 监控：云监控 + Spring Actuator

---

## 7. 暂时搁置（开发过程中定）

| 主题 | 搁置原因 |
|---|---|
| 消息队列 | MVP 无高并发 |
| 全文搜索（ES） | 任务量 < 1k，MySQL LIKE 够用 |
| 分布式锁 | 单机部署够用 |
| 链路追踪（Sleuth / SkyWalking） | MVP 无分布式 |
| Prometheus + Grafana | MVP 无大规模监控 |
| 对象存储 | 抽象层已留，二期再换 |
| WebSocket（实时通知） | 通知 MVP 用轮询，WS 二期 |
| 多租户 | 内部系统，单租户足够 |
| 国际化（i18n） | 全员中文 |
| 暗黑模式 | DESIGN.md 暂不支持 |
| token 验证优化 | MVP 调外部接口，二期可加本地 JWT 验签 |

---

## 8. 开发工具链

| 工具 | 用途 |
|---|---|
| **IDEA** | 后端开发（Evan 已有） |
| **VS Code / WebStorm** | 前端开发 |
| **DBeaver / Navicat** | 数据库管理 |
| **Postman / Apifox** | API 调试 |
| **Git + GitHub + Gitee** | 版本控制（已配置） |
| **Docker Desktop** | 本地容器化 |
| **Maven** | Java 依赖管理 |

---

## 9. 下一步

- [x] **§6 决策 1：SSO 集成方案已定**（公司插件 + 人员接口）✅
- [x] **§6 决策 2：富文本 TipTap**（MIT 开源可商用）✅
- [x] **§6 决策 3：MyBatis-Plus**（Apache 2.0）✅
- [x] **§6 决策 4：Spring @Scheduled（MVP）** ✅
- [x] **§6 决策 5：Mac mini + 云 ECS** ✅
- [ ] 写 v0.6：分模块拆解 + 数据库详细设计
- [ ] 写 v0.7：开发计划（按周/天排期）
- [ ] Phase 1 启动 MVP 开发

---

## 版本历史

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.5 | 2026-06-11 | 初稿：基于现有项目 + 需求 v0.4 的全栈技术选型 |
| v0.5.1 | 2026-06-11 | 确认鉴权走公司 SSO 插件 + 人员信息接口，TaskHub 不做登录 |
| v0.5.2 | 2026-06-11 | 锁定全部 5 个开放决策：TipTap / MyBatis-Plus / @Scheduled / Mac mini + 云 ECS，技术选型定稿 |
| v0.5.3 | 2026-06-11 | **修正**：UI 库从"不引入"改为"shadcn-vue + Reka UI"——避免代码量爆炸，仍保 MIT 商用 |
