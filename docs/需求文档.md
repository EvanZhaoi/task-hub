# TaskHub 需求文档 v0.5

> **项目代号**：TaskHub
> **文档版本**：v0.5
> **最后更新**：2026-06-12
> **状态**：待产品负责人确认
> **变更记录**：见文末"版本历史"

---

## 1. 背景与目标

### 1.1 背景
- 公司内部有大量开发任务，但开发资源**有限且分散**在各个部门
- 已有**虚拟货币制度**（≈ 真实奖金，关系员工收益）
- 缺乏市场化的资源调配机制，导致部分任务长期无法推进

### 1.2 目标
搭建一个**内部任务交易撮合平台**，用市场化的方式让"谁有活"和"谁有空"自然对接。

### 1.3 MVP 范围声明

| 类别 | MVP 做 | 留到二期 |
|---|---|---|
| 任务流转 | 发布、投标、选标、指名、完成确认 | — |
| 资金 | ❌ 不做（走内部系统） | — |
| 信用 | — | ✅ 信用评分 |
| 匹配 | — | ✅ 任务分类 + 技能匹配 + 推送 |
| 定价 | — | ✅ 历史成交均价 |
| 仲裁 | — | ✅ 平台介入机制 |
| 用户体系 | ✅ 对接外部接口 | — |
| 附件 | ✅ 文件上传 | — |
| 任务描述 | ✅ 富文本（WYSIWYG） | — |
| 搜索 | ✅ 关键词搜索（任务标题 + 描述） | 全文搜索 / 高级搜索语法 |
| 分页 | ✅ 任务大厅列表分页 | 老板视图也分页（暂以筛选代替） |
| 老板视图 | ✅ 全局甘特图 + 按付款账号筛选 | — |
| 个人视图 | ✅ 个人时间线（甘特图） | 高级数据汇总 |

---

## 2. 角色与人员体系

### 2.1 系统内角色

| 角色 | 描述 | 系统内权限 |
|---|---|---|
| **任务发布者** | 任何员工 | 浏览、发布、投标、确认完成、取消 |
| **接单开发者** | 任何员工 | 浏览、投标、协商、交付 |
| **老板** | 拥有全局视图权限 | 看所有任务的甘特图视图 |

> MVP 不区分"管理员"角色。付款账号的管理在**公司内部系统**进行，TaskHub 只引用账号 ID。

### 2.2 人员信息来源（关键决策）

**TaskHub 不维护本地用户表**，人员信息全部从**外部人员信息接口**获取：

- **不存**：用户姓名、工号、部门、邮箱等基础信息
- **只引用**：通过外部用户 ID 关联
- **缓存策略**：在 TaskHub 内部做短期缓存（建议 1 小时），减少对外部接口的调用
- **接口约定**（待补）：见 §13 遗留问题 L6

### 2.3 权限模型要点

- 权限与外部接口中的"部门 / 角色 / 职级"绑定
- MVP 阶段简化为：**所有已登录用户权限对等**，老板视图单独入口
- 二期可引入：部门隔离、职级限制等

---

## 3. 核心概念

| 概念 | 描述 |
|---|---|
| **任务（Task）** | 一笔开发工作的最小单位，从发布到完结有明确状态流转 |
| **投标（Bid）** | 开发者对应任务的报价响应 |
| **协作组（Collaboration Group）** | 多人协作投标时的一组开发者 |
| **付款账号（Payment Account）** | 标记任务的资金来源，平台只引用 ID |
| **附件（Attachment）** | 任务相关的文件（PRD、UI 稿、接口文档等） |
| **状态变更记录（Change Log）** | 任务进行中所有状态/字段变更的审计日志 |

---

## 4. 任务状态机

```
   ┌────────┐
   │ DRAFT  │  (草稿，发布者编辑中)
   └───┬────┘
       │ 发布
       ▼
   ┌────────┐
   │  OPEN  │  (招标中)
   └───┬────┘
       │ 超时无投标              │ 选标
       ▼                        ▼
   ┌────────┐              ┌──────────┐
   │ FAILED │              │ ASSIGNED │  (进行中)
   └───┬────┘              └────┬─────┘
       │ 复制新建                │ 发布者确认完成
       │                        ▼
       │                   ┌──────────┐
       └──────────────────►│COMPLETED │
                           └──────────┘

   DRAFT / OPEN / ASSIGNED → CANCELLED  (取消)
```

### 4.1 状态定义

| 状态 | 含义 | 终态？ |
|---|---|---|
| DRAFT | 草稿，未对外可见 | ❌ |
| OPEN | 招标中，对全员可见 | ❌ |
| ASSIGNED | 已选标，进行中 | ❌ |
| COMPLETED | 已完成 | ✅ |
| FAILED | 流标 | ❌（可复制新建） |
| CANCELLED | 已取消 | ✅ |

### 4.2 状态转换规则

| 起始状态 | 操作 | 结果状态 | 触发者 |
|---|---|---|---|
| DRAFT | 发布 | OPEN | 发布者 |
| DRAFT | 取消编辑 | CANCELLED | 发布者 |
| OPEN | 选标 | ASSIGNED | 发布者 |
| OPEN | 流标（超时无投标） | FAILED | 系统/定时 |
| OPEN | 撤回 | CANCELLED | 发布者 |
| ASSIGNED | 确认完成 | COMPLETED | 发布者 |
| ASSIGNED | 双方同意取消 | CANCELLED | 发布者+中标者 |
| FAILED | 复制新建 | DRAFT（新建一条） | 发布者 |
| * | 字段变更 | 状态不变，生成 Change Log | 双方 |

---

## 5. 核心流程

### 5.1 流程 A：任务发布（招标模式）

1. 发布者填写任务信息：标题、**富文本描述**、**付款账号**、预算金额、期望交期、招标截止时间
2. **发布者线下与付款账号管理员确认金额**（平台不拦截）
3. 点击"发布" → 状态变 `OPEN`，对全员可见

> 金额一旦发布后变更，需发布者**线下再次找管理员确认**。

#### 5.1.1 富文本描述规范
任务描述使用 **WYSIWYG 富文本编辑器**（原型用 Quill 1.3.7 snow 主题，生产可换），必须支持：

| 功能 | 必选 | 说明 |
|---|---|---|
| 加粗 / 斜体 / 下划线 / 删除线 | ✅ | 基础格式 |
| 1-3 级标题 | ✅ | 用于任务分段 |
| 有序 / 无序列表 | ✅ | 列验收点 / 需求点 |
| 引用块 | ✅ | 引用 PRD / 需求片段 |
| 行内代码 / 代码块 | ✅ | 贴示例代码 / 字段名 |
| 链接 | ✅ | 引用外部文档 |
| 表格 | ⏳ 二期 | 复杂数据展示 |
| 图片 | ⏳ 二期 | 贴 UI 稿等 |
| @ 提及人 | ⏳ 二期 | 拉人入任务 |

**数据格式**：以 **HTML 字符串** 存数据库（不存 Markdown）。前端渲染时**必须**走 XSS 过滤（推荐 DOMPurify）。

**长度限制**：建议 ≤ 50,000 字符（防滥用）。

### 5.2 流程 B：投标

#### 单人投标
- 填写：投标金额、交期、可选方案描述
- 同一任务对同一开发者**只能投标一次**（可撤回后重投）

#### 多人协作投标
- 主对接人 A 填写：投标金额、交期、方案
- 在"协作成员"中**从人员接口下拉选择**其他成员
- 协作成员只需要被加入，不需要二次确认（MVP 简化）

> 数据口径：多人协作投标使用独立协作成员关系表记录，不用 JSON 字段存列表。这样可以正常支持“我参与的投标 / 我参与的任务”查询和唯一性约束。

### 5.3 流程 C：选标

1. 发布者浏览所有投标（**互相不可见**，A 不知道 B 的报价）
2. 选中一个投标
3. **可调整**：最终金额、最终交期、任务明细
4. 系统自动通知中标者，**其他投标者状态变为 LOST**（无主动通知）
5. 任务状态变 `ASSIGNED`

### 5.4 流程 D：直接指名

1. 发布者填写任务信息 + **被指名人**（从人员接口选）+ 金额
2. 状态直接变 `ASSIGNED`，**无需被指名人接受**（MVP 简化）
3. 被指名人收到通知

> 数据口径：直接指名不强行生成投标记录，任务直接写入 `assigned_user_id`，并将实际参与人写入任务参与人关系表；招标选标才写 `assigned_bid_id`。

> 未来可加：被指名人拒绝 → 回到 OPEN。

### 5.5 流程 E：老板加塞 / 协商延期

> 老板有了新紧急任务，要求开发者放下手头的事先做新的。

1. 开发者接了新任务（`ASSIGNED`）
2. 开发者对**现有进行中的任务**发起"协商延期"
3. 线下与现有任务发布者沟通
4. 双方达成一致后，发布者点击"同意延期"
5. 系统创建 / 更新 Change Request，记录新的交期 + 原因 + 双方确认状态
6. 确认通过后任务状态保持 `ASSIGNED`，变更结果入 Change Log

> **不强制**：协商失败时，由双方自行找上司处理，平台不介入。

### 5.6 流程 F：任务变更 / 取消

#### 变更（进行中）
- 可变更字段：需求描述、最终金额、最终交期
- **必须双方确认**才生效；待确认阶段进入 Change Request
- 确认通过后的变更结果入 Change Log

#### 取消（任意状态）
- DRAFT / OPEN：发布者单方取消
- ASSIGNED：**必须双方同意**
- 取消后状态 `CANCELLED`
- **MVP 不做已做工时的折算**（不记录工时）

### 5.7 流程 G：完成确认

1. 开发者交付
2. 线下与发布者沟通
3. 发布者在系统上点击"确认完成"
4. 状态变 `COMPLETED`，变更记录入档

> MVP 不做超时自动机制。发布者不点完成 = 任务永远在 `ASSIGNED`，**双方自行找上司**处理。

### 5.8 流程 H：流标与重发

1. OPEN 状态超过招标截止时间，**无投标** → 状态变 `FAILED`
2. 发布者可点击"复制新建"
3. 复制原任务的**模板字段**（标题、描述、预算范围等），**不复制历史投标**
4. 新建任务进入 `DRAFT` 状态

### 5.9 流程 I：任务详情页布局

任务详情页由上至下分 4 个区块，**描述必须做大**（不再像副标题）：

1. **任务标题 + 关键元信息卡**（顶部）
   - 标题：30px 加粗
   - 右侧：状态徽章
   - 下方一行：发布者头像 + 预算 + 期望交期 + 创建时间
2. **需求描述卡**（中部，重点）
   - 顶部：灰色小字标头 "需求描述"
   - 描述正文：16px 字号 / 1.75 行高 / 富文本渲染
   - 是页面的视觉中心
3. **补充信息卡**（中下）
   - 4 列网格：预算 / 最终金额 / 最终交期 / 招标截止
   - 付款账号单独占 2 列
4. **附件 / 投标列表 / 变更记录 / 操作按钮**（底部，按需显示）

> **设计意图**：让开发者**第一眼**就看清需求，而不是看一堆元信息。这是 v0.2 → v0.3 的关键改动之一。

### 5.10 流程 J：任务搜索

#### 5.10.1 适用范围
| 视图 | 支持搜索 | 搜索字段 |
|---|---|---|
| 任务大厅（/） | ✅ | 标题 + 描述 |
| 老板视图（/boss） | ✅ | 标题 |
| 个人中心 | ⏳ 二期 | — |

#### 5.10.2 交互规格
- **位置**：筛选条右上方，搜索框宽度 220-240px
- **触发方式**：实时筛选（onChange，**不**用回车键）
- **匹配规则**：
  - 大小写不敏感
  - 子串匹配（LIKE '%query%'）
  - MVP 不做分词、不做模糊匹配
- **清空**：搜索框右侧有 ✕ 按钮，一键清空
- **结果**：
  - 命中：列表立即刷新
  - 零命中：显示 "没找到匹配 'XXX' 的任务" + 清空按钮
- **与状态筛选的叠加**：搜索 ∩ 筛选（AND 关系）
- **重置分页**：任何搜索变更重置到第 1 页

#### 5.10.3 性能与限制
- MVP 数据量在 1k 以内，**MySQL LIKE 即可**（不强求 ES）
- 超过 10k 任务或需要模糊匹配时，再考虑 Elasticsearch / Meilisearch
- 搜索关键词不存历史记录（无搜索建议）

### 5.11 流程 K：列表分页

#### 5.11.1 适用范围
| 视图 | 分页 | 页大小 |
|---|---|---|
| 任务大厅（/） | ✅ | 4 / 页（演示用，生产可调到 10-20） |
| 老板视图 | ⏳ 二期 | — |
| 个人中心 | ⏳ 二期 | — |

> **老板视图和个人中心**目前任务数小（MVP 数据量 < 10），用筛选代替分页。

#### 5.11.2 交互规格
- **位置**：列表底部，仅在总条数 > 页大小时显示
- **组件**（Linear 风格）：
  - 左侧："显示 1-4 / 共 8 条"
  - 右侧："‹ 上一页" + "第 N / M 页" + "下一页 ›"
- **行为**：
  - 第 1 页时"上一页"禁用（变灰）
  - 末页时"下一页"禁用
  - **筛选 / 搜索 变更重置到第 1 页**
  - 直接跳转到指定页（MVP 不做，超出页大小按末页处理）

#### 5.11.3 后端实现
- API：`GET /api/tasks?page=1&size=4&q=xxx&status=OPEN`
- 返回结构：
  ```json
  {
    "items": [...],
    "total": 8,
    "page": 1,
    "size": 4,
    "totalPages": 2
  }
  ```

---

## 6. 数据模型

> 这里只列核心字段，详细类型见开发阶段。

### 6.1 Task（任务表）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 主键 |
| title | string | 任务标题 |
| description | text | 任务描述 |
| payment_account_id | string | 付款账号 ID（外部引用） |
| budget | decimal | 预算金额 |
| final_amount | decimal | 最终成交金额（中标后填） |
| expected_delivery | date | 期望交期 |
| final_delivery | date | 最终交期（中标后填） |
| bidding_deadline | timestamp | 招标截止时间 |
| status | enum | DRAFT/OPEN/ASSIGNED/COMPLETED/FAILED/CANCELLED |
| complexity | enum | LOW 简单 / MEDIUM 中等复杂（默认）/ HIGH 高度复杂 |
| created_by | string | 发布者用户 ID（外部引用） |
| assigned_bid_id | string | 中标投标 ID（招标模式 ASSIGNED 后填） |
| assigned_user_id | string | 当前主负责人用户 ID（中标者或直接指名人） |
| created_at | timestamp | |
| updated_at | timestamp | |
| completed_at | timestamp | |

### 6.2 TaskAssignee（任务最终参与人）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 主键 |
| task_id | string | 关联任务 |
| user_id | string | 参与人用户 ID |
| role | enum | OWNER/COLLABORATOR |
| created_at | timestamp | |

### 6.3 Bid（投标表）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 主键 |
| task_id | string | 关联任务 |
| bidder_id | string | 投标者用户 ID（外部引用） |
| amount | decimal | 投标金额 |
| delivery_date | date | 投标交期 |
| proposal | text | 方案描述（可选） |
| status | enum | ACTIVE/WITHDRAWN/ACCEPTED/LOST |
| created_at | timestamp | |

### 6.4 BidCollaborator（投标协作人）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 主键 |
| bid_id | string | 关联投标 |
| user_id | string | 协作成员用户 ID |
| created_at | timestamp | |

### 6.5 AttachmentRef（附件引用）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 主键 |
| owner_type | enum | TASK/BID |
| owner_id | string | 任务 ID 或投标 ID |
| attachment_id | string | 外部附件 ID |
| uploaded_by | string | 上传者用户 ID |
| created_at | timestamp | |

### 6.6 TaskChangeRequest（任务变更请求）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 主键 |
| task_id | string | 关联任务 |
| change_type | enum | AMOUNT/DELIVERY/DESCRIPTION/CANCEL/EXTENSION |
| old_value | json | 变更前值 |
| new_value | json | 变更后值 |
| initiator_id | string | 发起人用户 ID |
| approver_id | string | 待确认人用户 ID |
| status | enum | PENDING/APPROVED/REJECTED/CANCELLED |
| reason | text | 原因（协商延期时必填） |
| responded_at | timestamp | 确认 / 拒绝时间 |
| created_at | timestamp | |

### 6.7 TaskChangeLog（任务变更记录）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 主键 |
| task_id | string | 关联任务 |
| change_type | enum | AMOUNT/DELIVERY/DESCRIPTION/CANCEL/EXTENSION |
| old_value | json | 变更前值 |
| new_value | json | 变更后值 |
| agreed_by | json | 双方用户 ID 列表 |
| request_id | string | 来源变更请求 ID（如有） |
| reason | text | 原因（协商延期时必填） |
| created_at | timestamp | |

### 6.5 视图层（不存表，按需聚合）

- **个人主页**：当前用户发布的任务 + 投标的任务 + 进行中的任务
- **老板甘特图视图**：所有任务 + 按时间线 + 按状态着色

---

## 7. 权限矩阵

| 操作 | 发布者 | 中标者 | 其他用户 | 备注 |
|---|---|---|---|---|
| 浏览任务（OPEN/历史） | ✅ | ✅ | ✅ | 全员公开 |
| 投标 | ✅ | ✅ | ✅ | 同任务一人一标 |
| 选标 | ✅ | - | - | 只能选自己发布的 |
| 直接指名 | ✅ | - | - | 发布者专属 |
| 协商延期 | ✅ | ✅ | - | 双方 |
| 变更字段 | ✅ | ✅ | - | 双方确认 |
| 取消任务 | ✅ | ✅ | - | 见 §5.6 |
| 确认完成 | ✅ | - | - | 发布者专属 |
| 查看他人投标 | - | - | - | **互相不可见** |
| 老板视图 | - | - | - | 老板专属 |

---

## 8. 通知规则（MVP 简化）

| 事件 | 通知对象 | 通知方式 |
|---|---|---|
| 任务发布 | 无 | — |
| 收到投标 | 发布者 | 站内消息 + 邮件 |
| 被选中 | 中标者 | 站内消息 + 邮件 |
| 未中 | 无 | （中标后自然知） |
| 状态变更 | 双方 | 站内消息 |
| 流标 | 发布者 + 原投标者 | 站内消息 |
| 完成 | 双方 | 站内消息 + 邮件 |
| 协商延期发起 | 对方 | 站内消息 |

> 企微 / 钉钉集成：二期。

---

## 9. 老板视图（甘特图）

### 9.1 入口
- 老板账号登录后，主导航有"老板视图"入口
- 普通用户**不可见**此入口

### 9.2 视图规格
- **图表类型**：甘特图（Gantt）
- **X 轴**：时间
- **Y 轴**：任务列表（默认按创建时间倒序）
- **任务条**：从任务创建到最终交期的时间跨度
- **颜色按状态**：
  - OPEN：蓝色（招标中）
  - ASSIGNED：橙色（进行中）
  - COMPLETED：绿色（已完成）
  - FAILED：灰色（流标）
  - CANCELLED：浅灰 + 删除线（已取消）

### 9.3 交互
- 点击任务条 → 跳转到任务详情
- 顶部筛选：按部门、按发布者、按状态
- 时间范围：默认最近 90 天，可选本周 / 本月 / 本季

### 9.4 数据范围
- 默认看**全部任务**
- 可按付款账号（部门）筛选

---

## 10. 个人主页

### 10.1 内容（MVP）
- **我的发布**：发布中 / 进行中 / 已完成 / 已取消 / 已流标（带计数 + 列表）
- **我的投标**：投标中 / 已中标 / 未中 / 已撤回（带计数 + 列表）
- **我接的**：进行中 + 已完成（带计数 + 列表）**+ 我的任务时间线（甘特图）**
- **信用等级**：MVP 显示"未启用"（Phase 2 启用）

### 10.2 我的任务时间线（个人甘特图）

位于"我接的" Tab **列表下方**。

- **数据范围**：仅当前用户接的任务（`ASSIGNED` + `COMPLETED`）
- **时间范围**：自动按用户任务计算（首任务 - 7 天 ~ 末任务 + 7 天）
- **颜色按状态**：
  - ASSIGNED：橙色（进行中）
  - COMPLETED：绿色（已完成）
- **交互**：
  - 任务条点击 → 跳任务详情
  - 横向滚动看跨月任务
- **空状态**：用户没接单时显示"还没有接单任务"提示

### 10.3 二期扩展
- 累计完成任务数、平均交期准时率、好评率
- 擅长领域（按历史任务分类聚合）
- 个人产出口碑统计

---

## 10. 个人主页

### 10.1 内容（MVP）
- **我的发布**：发布中 / 进行中 / 已完成 / 已取消 / 已流标（带计数 + 列表）
- **我的投标**：投标中 / 已中标 / 未中 / 已撤回（带计数 + 列表）
- **我接的**：进行中 + 已完成（带计数 + 列表）**+ 我的任务时间线（甘特图）**
- **信用等级**：MVP 显示"未启用"（Phase 2 启用）

### 10.2 我的任务时间线（个人甘特图）

位于"我接的" Tab **列表下方**。

- **数据范围**：仅当前用户接的任务（`ASSIGNED` + `COMPLETED`）
- **时间范围**：自动按用户任务计算（首任务 - 7 天 ~ 末任务 + 7 天）
- **颜色按状态**：
  - ASSIGNED：橙色（进行中）
  - COMPLETED：绿色（已完成）
- **交互**：
  - 任务条点击 → 跳任务详情
  - 横向滚动看跨月任务
- **空状态**：用户没接单时显示"还没有接单任务"提示

### 10.3 二期扩展
- 累计完成任务数、平均交期准时率、好评率
- 擅长领域（按历史任务分类聚合）
- 个人产出口碑统计

---

## 11. MVP 范围（最终版）

### ✅ 必须做
- 任务发布（招标）+ 直接指名
- 任务描述：富文本编辑器（WYSIWYG）
- 投标（单人 + 多人协作）
- 选标 + 金额/交期调整
- 任务进行中的变更与协商延期
- 完成确认
- 流标 + 复制新建
- 取消
- 站内消息通知
- 老板视图（甘特图 + 按付款账号筛选）
- 个人主页 + 个人时间线（甘特图）
- 任务详情页布局优化（描述为主体）
- 任务附件上传
- 对接外部人员信息接口
- **任务搜索（标题 + 描述，LIKE 匹配）**
- **任务大厅列表分页（默认 4 / 页）**
- **任务复杂度字段（LOW 简单 / MEDIUM 中等复杂 / HIGH 高度复杂）** v0.5 新增

### ❌ 不做（Phase 2+）
- 信用额度 / 评分
- 任务分类 / 标签 / 搜索
- 价格参考 / 历史均价
- 推送 / 订阅 / 邮件订阅
- 任务超时自动机制
- 平台仲裁 / 自动机制
- 管理员系统角色
- 付款账号系统集成
- 企微 / 钉钉 / IM 集成
- 公开案例库
- 任务工时记录与折算
- 数据分析 / 报表

---

## 12. 二期候选（按优先级，待排序）

1. 信用评分（最优先）
2. 任务分类 + 技能匹配 + 推送
3. 历史成交均价 / 定价辅助
4. 企微 / IM 集成
5. 平台仲裁机制
6. 任务超时自动机制
7. 数据分析

---

## 13. 待确认遗留问题

| # | 问题 | 状态 |
|---|---|---|
| L1 | 项目正式命名 | ✅ `TaskHub` |
| L2 | 多人协作成员来源 | ✅ 对接外部人员接口 |
| L3 | 直接指名是否需要接受 | ✅ MVP 自动生效 |
| L4 | 老板公告级任务 | ✅ 不做（搜索可代替部分场景） |
| L5 | 附件支持 | ✅ 支持文件上传 |
| L6 | 外部人员信息接口的具体协议 | ⏳ 需技术对接时确定 |
| L7 | 老板视图的 Y 轴排序默认 | ⏳ MVP 暂按创建时间倒序 |
| L8 | 文件上传大小限制 | ⏳ 建议 20MB/文件 |
| L9 | 任务是否需要"紧急程度"字段 | ⏳ MVP 不做（搜索已能定位"紧急"） |
| L16 | 任务复杂度字段（LOW/MEDIUM/HIGH）| ✅ v0.5 已加：表单必填、卡片 + 详情显示徽章、任务大厅按复杂度筛选（不影响定价） |
| L10 | 任务是否需要标签/优先级字段（为二期预留） | ⏳ MVP 不做 |
| L11 | 富文本编辑器选型 | ⏳ 原型用 Quill 1.3.7，生产可换 TipTap / Lexical / Slate / 自研 |
| L12 | 任务描述长度上限 | ⏳ 建议 ≤ 50,000 字符 |
| L13 | 任务描述 HTML 存储 vs Markdown 存储 | ⏳ 倾向 HTML（实现简单） |
| L14 | **搜索结果排序规则** | ⏳ 默认按创建时间倒序，二期可加"按相关度" |
| L15 | **老板视图 / 个人中心是否分页** | ⏳ 数据量大时再分（MVP 用筛选代替） |

---

## 14. 下一步

- [x] **v0.3** 需求定稿（包含原型验证后的 3 个新决策）✅
- [x] **v0.4** 需求定稿（增加搜索 + 分页）✅
- [ ] **v0.5** 技术选型（数据库 / 后端框架 / 前端框架 / 部署方案）
- [ ] **v0.6** 分模块拆解 + 开发计划（按周/天排期）
- [ ] **Phase 1** 启动 MVP 开发

---

## 版本历史

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1 | 2026-06-11 | 初稿（4 轮需求讨论） |
| v0.2 | 2026-06-11 | 增加：人员信息外部接口、文件上传、老板甘特图视图、个人主页 |
| v0.3 | 2026-06-11 | 增加：富文本描述、需求描述布局优化（描述为主体）、个人时间线甘特图 |
| v0.4 | 2026-06-11 | 增加：任务搜索（标题+描述）、任务大厅列表分页、老板视图按付款账号筛选 |
| v0.5 | 2026-06-12 | 增加：任务复杂度字段（LOW/MEDIUM/HIGH；表述从"难易程度"改为"复杂程度"，值为"简单/中等复杂/高度复杂"；表单必填+徽章显示+任务大厅筛选） |
