# 15-基础 UI 与日期组件修正

## 本章目标

本章记录一次基础体验修正，不开发新的任务业务功能。

修正目标：

1. Button 鼠标样式统一放到 shadcn 基础组件中。
2. 发布任务弹窗字段布局更稳定，不被长文本撑破。
3. 发布任务表单不再直接使用原生 `date` / `datetime-local` 输入框。
4. 新增通用 `DatePicker` 和 `DateTimePicker`，后续业务表单统一复用。
5. 修正文档和代码中的错误 `Card as="..."` 示例。
6. 明确流标定时任务未来使用 Laravel Scheduler，不为每个任务创建独立 Cron。

## 前置条件

请先完成：

- 第 11 章 shadcn/ui 接入。
- 第 12 章发布任务模态框。
- 第 13 章开发任务清单。

当前项目已经有：

```text
resources/js/components/ui/button.tsx
resources/js/components/ui/card.tsx
resources/js/Pages/Tasks/Index.tsx
```

## 本章最终效果

完成后：

- 所有 `Button` 默认显示 `cursor-pointer`。
- `Button disabled` 显示 `cursor-not-allowed`。
- 业务页面不需要重复给按钮写鼠标样式。
- 发布任务弹窗中：
  - 预算金额 / 复杂度各占一半。
  - 期望交付日期 / 招标截止时间各占一半。
  - 其他字段占满整行。
  - 所有控件都是 `w-full`。
  - 字段容器都是 `min-w-0`。
  - 付款账号长文本不会撑破 Dialog。
- 发布任务表单使用：
  - `components/common/DatePicker.tsx`
  - `components/common/DateTimePicker.tsx`
- 日期提交格式仍兼容现有后端：
  - `expectedDelivery`：`YYYY-MM-DD`
  - `biddingDeadline`：`YYYY-MM-DDTHH:mm`

## 实际执行命令

命令执行目录：

```text
task-hub 项目根目录
```

安装 shadcn Popover 和 Calendar 依赖：

```bash
npm install @radix-ui/react-popover react-day-picker --registry=https://registry.npmjs.org/
```

为什么这样做：

- `@radix-ui/react-popover` 提供浮层交互能力。
- `react-day-picker` 提供日历选择能力。
- TaskHub 的 `DatePicker` / `DateTimePicker` 只组合基础 UI，不直接把业务逻辑写进 shadcn 基础组件。

验证命令：

```bash
npm run typecheck
npm run build
php artisan test
```

## 涉及文件

| 文件 | 作用 |
|---|---|
| `package.json` | 增加 Popover 和 Calendar 依赖 |
| `package-lock.json` | 锁定依赖版本 |
| `resources/js/components/ui/button.tsx` | 统一 Button 鼠标样式 |
| `resources/js/components/ui/card.tsx` | 修正为标准 Card，不支持 `as` 属性 |
| `resources/js/components/ui/native-select.tsx` | 统一 Select 为 `w-full min-w-0 truncate` |
| `resources/js/components/ui/popover.tsx` | 新增 shadcn 风格 Popover 基础组件 |
| `resources/js/components/ui/calendar.tsx` | 新增 shadcn 风格 Calendar 基础组件 |
| `resources/js/components/common/DatePicker.tsx` | 新增业务通用日期选择器 |
| `resources/js/components/common/DateTimePicker.tsx` | 新增业务通用日期时间选择器 |
| `resources/js/Pages/Tasks/Index.tsx` | 发布任务弹窗改用日期组件并修正布局 |
| `resources/js/Pages/Sso/Callback.tsx` | 修正 `Card as="section"` 用法 |
| `docs/development/09-SSO授权码模式接入.md` | 修正错误 Card 示例 |
| `docs/development/11-shadcn-ui接入.md` | 修正 Button 和 Card 示例 |
| `docs/development/12-发布任务模态框.md` | 修正日期组件示例 |
| `docs/development/13-开发任务清单.md` | 补充 Laravel Scheduler 定时流标说明 |

## 每一步操作

### 1. 统一 Button 鼠标样式

修改：

```text
resources/js/components/ui/button.tsx
```

关键代码：

```tsx
const buttonVariants = cva(
    // cursor-pointer 放在 Button 基础组件中，业务页面不需要为每个按钮重复写。
    // disabled 时使用 cursor-not-allowed，用户能明确知道按钮当前不可操作。
    'inline-flex h-10 cursor-pointer items-center justify-center whitespace-nowrap rounded-md text-sm font-medium transition-colors outline-none disabled:cursor-not-allowed disabled:opacity-50',
);
```

为什么这样做：

- 鼠标样式属于所有按钮的通用体验。
- 放在基础组件里，后续业务页面不会重复写 `cursor-pointer`。
- 不能保留 `disabled:pointer-events-none`，否则浏览器可能无法在禁用按钮上显示 `cursor-not-allowed`。

### 2. 修正 Card 标准用法

修改：

```text
resources/js/components/ui/card.tsx
```

关键代码：

```tsx
export function Card({ className, ...props }: ComponentProps<'div'>) {
    // Card 只负责基础边框、圆角、背景；内边距交给 CardContent 或页面控制。
    // 保持标准 shadcn 写法：Card 自身是 div；需要 article/section 语义时在外层包语义标签。
    return <div className={cn('rounded-lg border border-[#e5e7eb] bg-white', className)} {...props} />;
}
```

为什么这样做：

- shadcn 的 `Card` 默认是 `div`。
- `Card as="article"` 不是标准 shadcn 写法，文档里继续这样写会误导。
- 如果需要 HTML 语义，可以写成：

```tsx
<article>
    <Card>
        {/* 任务内容 */}
    </Card>
</article>
```

### 3. 新增 Popover 基础组件

新增：

```text
resources/js/components/ui/popover.tsx
```

作用：

- 只负责浮层打开、关闭、定位。
- 不包含任务、投标、交付等业务逻辑。
- 后续日期选择器、筛选菜单、更多操作菜单都可以复用。

### 4. 新增 Calendar 基础组件

新增：

```text
resources/js/components/ui/calendar.tsx
```

作用：

- 基于 `react-day-picker` 封装日历。
- 统一 TaskHub 的日历样式。
- 业务组件只传 `selected` 和 `onSelect`。

注意：

- `react-day-picker` v10 的 `classNames` 字段和旧示例不同。
- 当前代码使用 `month_caption`，不要写旧版的 `caption`。

### 5. 新增 DatePicker

新增：

```text
resources/js/components/common/DatePicker.tsx
```

作用：

- 替代业务页面中的原生日期输入。
- 统一提交 `YYYY-MM-DD`。
- 只负责一个自然日，不处理小时和分钟。

为什么放在 `components/common`：

- 它不是 shadcn 基础组件。
- 它是 TaskHub 业务页面会反复使用的组合组件。
- 后续任务交期、变更交期、交付日期都可以复用。

### 6. 新增 DateTimePicker

新增：

```text
resources/js/components/common/DateTimePicker.tsx
```

作用：

- 用于需要具体时间点的字段，例如招标截止时间。
- 日期部分复用 `DatePicker`。
- 小时支持 `0-23`。
- 分钟支持 `0-59`。
- 不限制为 5 分钟或 15 分钟步长。

提交格式：

```text
YYYY-MM-DDTHH:mm
```

这个格式和原来的 `datetime-local` 一致，所以后端 `StoreTaskRequest` 不需要改。

### 7. 修正发布任务弹窗布局

修改：

```text
resources/js/Pages/Tasks/Index.tsx
```

布局规则：

```text
任务标题：100%
任务描述：100%
预算金额 / 复杂度：50% / 50%
期望交付日期 / 招标截止时间：50% / 50%
付款账号：100%
附件 ID：100%
```

关键代码：

```tsx
<div className="grid grid-cols-1 gap-4 sm:grid-cols-2">
    <Field label="预算金额" message={form.errors.budget} required>
        <Input className="w-full" />
    </Field>

    <Field label="复杂度" message={form.errors.complexity} required>
        <NativeSelect className="w-full" />
    </Field>
</div>
```

`Field` 容器：

```tsx
function Field({ children, label, message, required = false }: FieldProps) {
    return (
        <label className="block min-w-0 space-y-1.5">
            {/* 字段标题、控件、错误提示 */}
        </label>
    );
}
```

为什么加 `min-w-0`：

- CSS Grid / Flex 子项默认最小宽度可能由内容撑开。
- 付款账号名称很长时，如果父容器没有 `min-w-0`，Dialog 会被撑破。
- `min-w-0` 允许子项在可用宽度内收缩。

### 8. 修正付款账号 Select 长文本

修改：

```text
resources/js/components/ui/native-select.tsx
```

关键代码：

```tsx
<select
    className={cn(
        'h-10 w-full min-w-0 truncate rounded-md border border-[#d1d5db] bg-white px-3 text-sm text-[#374151] outline-none transition-colors',
        className,
    )}
/>
```

为什么这样做：

- `w-full` 让 Select 只占父容器宽度。
- `min-w-0` 允许它在 Grid/Dialog 里收缩。
- `truncate` 让选中的长文本省略，不把 Dialog 撑宽。

### 9. 补充流标定时任务规范

第 13 章保留原来的定时任务条目，不新增重复条目。

补充规则：

```text
Laravel Scheduler 每分钟执行一次
↓
扫描 OPEN && bidding_deadline <= now() 的任务
↓
调用 ExpireOpenTasksService
↓
Service 内部再次锁定 Task 并校验没有 ACTIVE Bid
↓
符合条件才改为 FAILED 并写 TASK_FAILED
```

关键要求：

- 不为每个任务单独创建 Cron。
- Scheduler 只负责定时触发。
- `ExpireOpenTasksService` 负责业务判断和事务。
- 处理逻辑必须幂等。
- 已经不是 `OPEN` 的任务不能重复处理。
- 已经有 `ACTIVE` 投标的截止任务不能自动流标，只能派生显示“待选标”。

## 如何验证

执行：

```bash
npm run typecheck
npm run build
php artisan test
```

手动检查：

1. 打开 `/tasks`。
2. 点击“发布任务”。
3. 确认预算金额和复杂度左右各半。
4. 确认期望交付日期和招标截止时间左右各半。
5. 选择一个很长的付款账号，Dialog 不应被撑宽。
6. 选择日期后，提交给后端的字段仍为：
   - `expectedDelivery = YYYY-MM-DD`
   - `biddingDeadline = YYYY-MM-DDTHH:mm`
7. 鼠标移动到普通按钮时是可点击手型。
8. 鼠标移动到禁用按钮时是不可点击样式。

## 常见错误

| 错误 | 原因 | 处理 |
|---|---|---|
| `caption does not exist in type ClassNames` | `react-day-picker` v10 不再使用旧版 `caption` className | 使用 `month_caption` |
| Dialog 被长账号名称撑宽 | 父容器或 Select 没有 `min-w-0` / `truncate` | 检查 `Field` 和 `NativeSelect` |
| 日期提交后后端校验失败 | 提交格式不是 `YYYY-MM-DD` 或 `YYYY-MM-DDTHH:mm` | 检查 `DatePicker` / `DateTimePicker` 的 `onChange` |
| disabled 按钮不显示禁用光标 | Button 中仍有 `disabled:pointer-events-none` | 移除该 class，保留 `disabled:cursor-not-allowed` |
| `Card as` 报 TypeScript 错误 | 标准 Card 不支持 `as` 属性 | 用外层 `article` / `section` 包住 `Card` |

## 本章总结

本次修正只处理基础 UI 和文档一致性，没有新增业务功能。

关键原则：

- 通用交互样式放到基础组件。
- 业务表单使用 common 组合组件，避免每个页面重复处理日期格式。
- shadcn 基础组件保持通用，不写 TaskHub 业务逻辑。
- 文档示例必须和真实代码保持一致。
- 定时任务使用 Laravel Scheduler 统一扫描，不为每个业务对象创建独立 Cron。

## 下一章入口

下一步可以继续进入业务开发。

建议优先：

1. 将发布任务事务整理到 `CreateTaskService`。
2. 新增任务详情页。
3. 开始投标功能。
