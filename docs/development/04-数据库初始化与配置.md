# 04-数据库初始化与配置

## 1. 本章目标

使用 `database/schema.sql` 初始化 MySQL 8，并让 Laravel 连接该数据库。

## 2. 前置条件

- MySQL 8 可用。
- 已创建或准备创建 `taskhub` 数据库。
- 已复制 `.env`。

## 3. 本章最终效果

`taskhub` 数据库中存在 8 张核心表，Laravel 可以通过 Eloquent 查询。

## 4. 实际执行命令

MySQL CLI：

```bash
mysql -uroot -p < database/schema.sql
php artisan config:clear
php artisan db:show
php artisan tinker
```

Tinker 中执行：

```php
App\Models\Task::query()->count();
```

## 5. 命令执行目录

`mysql -uroot -p < database/schema.sql` 在项目根目录执行，因为 SQL 文件路径是相对路径。

## 6. 命令作用

- `database/schema.sql`：创建 `taskhub` 数据库和 8 张核心表。
- `php artisan config:clear`：清理 Laravel 配置缓存。
- `php artisan db:show`：检查 Laravel 当前数据库连接。
- `php artisan tinker`：进入 Laravel 交互式 REPL。

## 7. 预期输出或生成文件

`php artisan db:show` 应显示 MySQL 连接和数据库名。

Tinker 中：

```text
> App\Models\Task::query()->count();
= 0
```

空库返回 `0` 是正常的。

## 8. 逐文件修改过程

### 8.1 修改 `.env`

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=taskhub
DB_USERNAME=root
DB_PASSWORD=your_password
```

### 8.2 不创建 migration

当前不新增业务 migration。Laravel 默认 users/cache/jobs migration 已删除。

### 8.3 使用 VS Code 数据库插件

如果不用 MySQL CLI，可以：

1. 安装 VS Code 数据库插件。
2. 新建 MySQL 连接。
3. 打开 `database/schema.sql`。
4. 连接到 MySQL 8 后执行整份 SQL。

## 9. 修改前后的关键代码

`.env` 修改前可能是：

```env
DB_CONNECTION=sqlite
```

修改后必须是：

```env
DB_CONNECTION=mysql
DB_DATABASE=taskhub
```

## 10. 核心原理解释

TaskHub 当前以 `database/schema.sql` 作为数据库结构标准。Laravel Migration 暂不用于修改业务结构，避免代码和已确认 SQL 发生分叉。

Eloquent Model 只负责映射已存在的表。

## 11. 与 Spring Boot 的概念对照

| Laravel | Spring Boot |
|---|---|
| `.env` DB 配置 | `spring.datasource.*` |
| `php artisan db:show` | 检查 DataSource |
| `php artisan tinker` | JShell / 临时调试 Controller 外代码 |
| Eloquent Model | JPA Entity + 查询能力 |

## 12. 验证步骤

```bash
php artisan config:clear
php artisan db:show
php artisan tinker
```

Tinker：

```php
App\Models\Task::query()->count();
App\Models\Task::query()->where('status', 'OPEN')->count();
```

## 13. 常见错误与解决方法

| 错误 | 处理 |
|---|---|
| `Access denied for user` | 检查 `.env` 用户名和密码 |
| `Unknown database taskhub` | 先执行 `database/schema.sql` |
| `could not find driver` | 启用 `pdo_mysql` |
| `.env` 改了不生效 | 执行 `php artisan config:clear` |
| 表不存在 | 确认执行的是当前仓库的 `database/schema.sql` |

## 14. 操作检查清单

- [ ] `.env` 使用 MySQL
- [ ] 已执行 `database/schema.sql`
- [ ] 未运行默认 Laravel migration
- [ ] `php artisan db:show` 成功
- [ ] `Task::query()->count()` 成功

## 15. 本章对应的实际项目文件

- `database/schema.sql`
- `.env.example`
- `config/database.php`
- `app/Models/Task.php`

## 16. 下一章入口

继续阅读 [05-Inertia与React初始化](./05-Inertia与React初始化.md)。
